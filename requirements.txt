# PTDTransformer pipeline dependencies.
#
# Developed and run on Google Colab (Ubuntu 22.04, Python 3.11, single T4 GPU;
# CPU also works — the model is small). The versions below match that Colab
# environment; nearby versions are expected to work.

numpy==1.26.4
pandas==2.2.2
pyarrow==17.0.0
scikit-learn==1.5.2
torch==2.5.1

# Analysis / plotting (notebook only)
matplotlib==3.9.2
seaborn==0.13.2
tqdm==4.66.5

# Notebook tooling (optional, for running notebooks/ locally)
jupyter==1.1.1
nbformat==5.10.4
